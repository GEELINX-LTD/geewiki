# 测试读数台账（自 README 表格中迁出）

> **这一页是什么**：历次「全仓测试读数」的逐包明细。它原本是 README 「快速开始」命令表里
> **`pnpm test` 那一行的表格单元格**，经过一轮轮追加（每轮都注明"上一读数已被本行取代"，
> 但为"维持台账连续性"都保留着），最后单格约 6900 字节、整行约 8500 字节。
>
> **两件必须记下的事**：
> 1. 该格正文里含**未转义的 `|`**，而 Markdown 表格正是用它分列——于是这一行被解析成 **5 列**，
>    出现在一张只有 2 列的表格里。**这行本来就是坏掉的**，不只是"太长"。
> 2. 台账的读写成本随批次数线性增长，而其中**只有最后一组读数有效**。
>
> 整改：正文**逐字迁出**到这里（内容未删改——连"已被本行取代"的旧读数一起保留，它们记录了每批的增量），
> README 那一行改为**当前读数 + 指向本页**。当前读数请以 README「质量基线」或实跑为准。

---

## `pnpm test` 的历史读数台账

运行单元测试（`pnpm -r --no-bail --if-present run test`，各包的 `test` 脚本都是 `node --import tsx --test test/*.test.ts`）。**当前实测（SSO 界面归还插件批）：1904/1904 全绿（24 个测试包，0 fail）**，取数 `2026-09-15`（工作树含未提交改动）；逐包（本批增量）：`packages/plugin-ai-assistant` **220**（揭幕方向批 +1：`uiDockMotion.test.ts` **12** 例，新增"裁剪盒必须锚底 + 面板不得被压缩"；工具活动收行批 +6：`uiDockToolRun.test.ts`——摘要文案边界（去重/次数/失败点名）/默认收起/流式铺开/一轮结束复位/受控 aria-expanded/箭头旋转与 reduced-motion；自动置底批 +7：`uiDockScroll.test.ts`——`isAtBottom` 边界/ref 与 onScroll/send 强制跟随/依赖只能是内容指纹/禁 window.scrollTo 与 smooth）、`packages/web` **761**（本页目录批 +1：`readingLayout.test.ts` **11** 例含新增的"sticky 包含块必须撑高"；插件依赖图批 +28：`pluginGraphPlan.test.ts` **16**、`pluginGraphHover.test.ts` **6**、`pluginTempChanges.test.ts` **6**——交叉削减/可见交叉/精修/上游链方向/压暗判定；`pluginGraphHover.test.ts` **6**——原生 title 禁令/正强调/压暗幅度/离开防抖/面板措辞）、`packages/manager` **144**（+9：`runtime-disable.test.ts`——不落盘/重启恢复/持久化/激活即清登记/依赖方守卫/会话层不受影响/图节点透出）；上一读数逐包：`packages/web` **732**、`packages/plugin-ai-assistant` **206**（联网搜索批 +8：依据分档 `'kb' | 'web'` 的新用例；动画批 +7→**+11**：`uiDockMotion.test.ts`；图标批 +5：`uiDockIcons.test.ts`；点外部收起批 +3：`uiDockDismiss.test.ts`）、**`packages/plugin-ai-web-search` 32**（本批新增：`anysearch.test.ts` 17 例 HTTP 契约/失败分类/正文配额/清洗 + `plugin.test.ts` 15 例真 cordis 工具总线行为——含 **0 命中不声明依据**与**关掉正文就必须真的剥掉**两条判据）、`packages/manager` **135**、`packages/plugin-wiki` **114**（含本批新增 `managed-save.test.ts` **7** 例）、`packages/plugin-llm` **92**、`packages/plugin-openai` **72**、`packages/server` **66**、`packages/plugin-ai-journal` **49**、`packages/plugin-search` **42**、`packages/plugin-ai-summary` **38**、`packages/plugin-ai-pages` **34**、`packages/plugin-ai-admin` **33**、`packages/core` **26**、`packages/plugin-ai-kb` **23**、`packages/db-postgres` **20**、`packages/plugin-auth` **18**、`packages/plugin-oidc` **17**、`packages/plugin-ai-tools` **17**、`packages/plugin-ai-writing` **15**、**`packages/plugin-builtin-docs` 14**（内置文档批新增：`plugin.test.ts` 9 例真策略层行为——含 **tier 物化**（hidden 双向翻动即时刷 `blocks.tier`）与**卸载补刷**，+ `content.test.ts` 5 例正文守卫，正文现从 `content/*.md` 装载）、`packages/plugin-authz` **4**、`packages/plugin-ai-nav` **4**、`packages/plugin-org` **3**。**上一读数（底部对话条丝滑/焦点/抽搐批）1806/1806（24 个包）已被本行取代**；更早的内置文档批读数 1747/1747（23 个包）也已被取代；相对它的增量：联网搜索批 **+32（新增整包）+8**，动画批 **+11**，图标批 **+5**，点外部收起批 **+3**；`web` 732 与 `ai-assistant` 相对 P8 读数（728/172）的上浮来自工作树里先于本批存在的未提交改动，与本批无关（本批只给 `ai-assistant` 补了依据分档的用例）。**再上一读数（P8，1715/1715，取数 `2026-09-15`）的逐包明细保留在下方以维持台账连续性**：`packages/web` **728**、`packages/plugin-ai-assistant` **172**、`packages/manager` **135**、`packages/plugin-wiki` **107**、`packages/plugin-llm` **92**、`packages/plugin-openai` **72**、`packages/server` **66**、`packages/plugin-ai-journal` **49**、`packages/plugin-search` **42**、`packages/plugin-ai-summary` **38**、`packages/plugin-ai-pages` **34**、`packages/plugin-ai-admin` **33**、`packages/core` **26**、`packages/plugin-ai-kb` **23**、`packages/db-postgres` **20**、`packages/plugin-auth` **18**、`packages/plugin-oidc` **17**、`packages/plugin-ai-tools` **17**、`packages/plugin-ai-writing` **15**、`packages/plugin-authz` **4**、`packages/plugin-ai-nav` **4**、`packages/plugin-org` **3**；**历史读数（1478/1478 → 1817/1817）已被本行取代**，保留在下方以维持台账连续性：**1478/1478 全绿（16 个包，0 fail）**，取数 `2026-09-14T21:16+08:00`（工作树含未提交改动）；逐包：`packages/web` **697**、`packages/manager` **135**、`packages/plugin-wiki` **101**、`packages/plugin-ai-qa` **122**、`packages/plugin-ai-writing` **52**、`packages/plugin-llm` **92**、`packages/plugin-openai` **72**、**`packages/plugin-ai-tools` 17**、**`packages/plugin-ai-kb` 20**、`packages/server` **66**、`packages/plugin-search` **42**、`packages/db-postgres` **20**、`packages/plugin-auth` **18**、`packages/plugin-oidc` **17**、`packages/plugin-authz` **4**、`packages/plugin-org` **3**。**上一读数 1276/1276（13 个包，取数 `2026-09-13T23:49+08:00`，HEAD `98b4618`）已被本行取代**；本批的增减都是结构变化的直接后果：**+** `plugin-ai-qa`（122 例，含 `budget` 与流式/契约用例）与 `plugin-ai-writing`（52 例，含 `plugin.test.ts` 的 manifest 守卫）、`plugin-llm` **新增** `degrade.test.ts`（**9** 例：`CODE_TO_REASON` 对 `LlmErrorCode` 全量覆盖、`search_unavailable` 钉在 `DegradedReason` 之外、`noModelDegraded`、`listRouteInfos` 对 `label`/`model` 脱敏）、两插件界面侧的镜像守卫（`uiPanel` **33** 例、`uiToolbar` **28** 例）、`web` **新增** `pluginUi.test.ts`（**13** 例：入口表解析 / 声明可见性 / 懒加载分派的咬合）；**−** 宿主侧随界面一并删除的四个测试文件（按文件计自 `HEAD`：`aiStreamPlan` 18、`assistPlan` 12、`degradedReason` 4、`askPanel` 2，共 36 条）；旧包 `plugin-ai` 的 84 例不是"删除"而是**随拆分迁入**上述两包（`packages/plugin-ai/test/*` 在 git 里显示为改名）。**以下为上一批（1276/1276，13 个包）的逐包读数与用例变化，保留以维持台账连续性，已被上面那行取代**：`packages/web` **687**（含顶栏主题守卫 2 例）、`packages/manager` **133**（含本批新增 `secrets` 8 例）、`packages/plugin-wiki` **101**、`packages/plugin-ai` **84**（含本批新增 `budget` 4 例）、`packages/server` **66**、`packages/plugin-llm` **51**（含本批新增 `settings` 10 例）、`packages/plugin-openai` **50**、`packages/plugin-search` **42**、`packages/db-postgres` **20**、`packages/plugin-auth` **18**、`packages/plugin-oidc` **17**、`packages/plugin-authz` **4**、`packages/plugin-org` **3**。**本批的用例变化**：`plugin-llm` 新增"统一接入配置"（schema 角色 / `extraBody` / 设置推导 / 密钥优先级 / `/api/llm/providers` 端点）10 例；`manager` 新增"写一次、不可回读字段"端到端 8 例（值进密钥文件、清单无密钥、读出脱敏、留空不修改、`clearSecrets` 清除）；`plugin-openai` 新增"设置现读 / 思考强度 / 额外请求体 / 配置热更新后适配器路由不丢"用例，并**删除** `test/credential-guard.test.ts`（密钥形态守卫随配置一起搬到 `@geewiki/llm`，其形态清单已被 `packages/plugin-llm/test/credentials.test.ts` 完整覆盖）；`plugin-ai` 新增 `budget`（上下文预算推导）4 例；`web` 新增 `configSchemaRoles`（`role: secret` / `llm-provider` 的翻译层）5 例；`web` 另新增**顶栏主题守卫 2 例**（`designSystem.test.ts`：① 全仓源码不得回流 `text-white` / `bg-white` 这类"为深色底写死"的类名，② `--gw-code-bg` / `--gw-code-ink` 必须与 `--gw-header-*` 解耦、且 `--gw-header-bg` 必须在浅色 `:root` / `.dark` / `prefers-color-scheme` **三处**各赋值一次）。<br>**注意 `packages/core` / `packages/db-sqlite` / `packages/plugin-echo` / `packages/plugin-editor-plain` / `plugins/hello-geewiki` 没有 `test` 脚本**（`--if-present` 跳过）。历史口径（134/134 三包 → 242/242 → 266/266 → 308/308 → 746/746 → 1105/1105 → 1274/1274 → 1276/1276）均已被本行取代，逐条记录见 [docs/plugin-platform-plan.md](docs/plugin-platform-plan.md) 第 9 节
